package queue;

import java.util.Iterator;
import java.util.LinkedList;
import java.util.ListIterator;
import java.util.Queue;
import java.util.Stack;

import static java.lang.System.*;

public class PalinList {
	
	Queue<String> q;
	Stack<String> s;
	
	public PalinList(String str) {
		q = new Queue(str.split(" "));
	}

	public static boolean isPalin(Queue<Integer> q){
		boolean z=true;
		ListIterator<Integer> iter = (ListIterator<Integer>) q.iterator();
		while(iter.h){
			iter.next();
		}
		while(iter.){
			if(q.poll().equals(q)){
				
			}
		}
		return z;
	}
	
	
	
}
